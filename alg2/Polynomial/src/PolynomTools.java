public class PolynomTools {
    public static Polynom addPolynoms(Polynom... polynoms)
    {
        for (Polynom currentPolynom : polynoms)
        {
            int[] currentCoefficients = currentPolynom.getCoefficients();
            // todo
        }
    }
}
